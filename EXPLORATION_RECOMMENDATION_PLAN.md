# 面向科学数据探索的下一步推荐：基于 NEX-GDDP CMIP6 子集的原型方案

> 目标：在不下载 28 TB 全量数据的前提下，先完成一个可运行、可解释、可评估的 one-step-ahead exploration 原型；验证后再迁移到 DYAMOND C1440-LLC2160 海气耦合数据。
>
> 数据入口：<https://sciviscontest2026.github.io/data/home#data3>
>
> 仓库：<https://github.com/azccjl/SCV>

## 1. 研究问题与边界

### 1.1 核心问题

当分析者已经选择了一个变量、时间段、空间区域和统计操作时，系统能否从大量可能操作中推荐少量“下一步值得探索”的候选，并用数据证据解释推荐原因？系统的职责是缩小搜索空间、支持判断和迭代，而不是替专家给出全局最优路径。

### 1.2 第一阶段明确不做的事

- 不下载或处理 28 TB 全量数据。
- 不把 LLM 当作数值分析器；统计量、相关系数和成本由可复现代码计算。
- 不声称 NEX-GDDP 能直接验证 Kuroshio 海气耦合机制。它只用于验证推荐框架、交互流程和评价指标。
- 不一开始做多 Agent、复杂强化学习或全自动分析。先完成单步候选生成、排序、解释、用户选择和回退。

## 2. 为什么先用 data3

赛事页面的 data3 是 NEX-GDDP CMIP6 数据：0.25° x 0.25° 网格，约 25 km；包含 1950--2014 历史模拟和 2015--2100 未来投影；页面列出的变量包括 `tas`、`pr`、`rsds`、`hurs`、`rlds`、`sfcWind`、`tasmin`、`tasmax`，空间形状为 `(600, 1440)`。页面列出的可用模型包括 ACCESS-CM2、CanESM5、CESM2、GFDL-ESM4、IPSL-CM6A-LR、MIROC6、MPI-ESM1-2-HR、MRI-ESM2-0 等。

它适合作为原型数据，因为变量语义和单位清楚、网格规则、时间尺度容易切片、空间范围容易限制，且可构造多模型、多情景和历史/未来对比。需要注意：data3 的网页只展示单时刻二维变量形状，实际下载时必须检查对象存储的时间维度、文件命名、缺测值和坐标方向，不能仅凭网页表格推断完整文件结构。

## 3. 最小可行研究闭环

```text
科学问题/兴趣
      |
当前分析状态 S=(变量、区域、时间窗、模型、情景、统计操作)
      |
生成候选动作 A：换变量 / 换时间窗 / 换区域 / 换模型或情景 / 换统计量
      |
快速执行候选 -> 计算证据、相关性、新颖性、成本、重复度
      |
候选排序 + 推荐原因 + 不确定性 + 可预览小图
      |
专家接受、拒绝、编辑或回退
      |
记录状态转移 (S,A,S')，重新推荐下一步
```

第一版只推荐 3 个候选。每个候选必须可以被用户一键执行，并显示“改变了什么、为什么推荐、代价多大、证据有多稳定”。

## 4. 数据子集与实验设计

### 4.1 推荐的初始子集

先选择一个 10° x 20° 左右的区域、一个模型、一个情景和 10--20 年时间窗。建议使用日本东部/黑潮延伸区附近的区域作为迁移友好的原型区域，例如 `130°E--150°E, 25°N--40°N`；第一轮也可使用全球粗采样来验证代码。

建议的第一组变量：

| 变量 | 单位 | 用途 |
|---|---|---|
| `tas` | K | 近地面气温 |
| `pr` | kg m-2 s-1 | 降水 |
| `sfcWind` | m s-1 | 近地面风速 |
| `hurs` | % | 近地面相对湿度 |

首轮只下载日尺度/可用的最小时间范围，并保留原始数据、坐标、模型、情景和版本元数据。原始文件不提交 GitHub；仓库只提交代码、配置、数据字典、派生小样例和结果摘要。

### 4.2 两个层次的实验

1. **合成任务验证**：人为设定目标，例如“寻找高风速且高降水的区域”，检查系统能否推荐更换变量、扩大/缩小区域、改变时间窗等动作。
2. **领域启发任务**：围绕黑潮区域提出“暖异常是否伴随风和降水变化”等弱假设，只做探索性关联，不作因果结论，并由导师/海洋专家审阅候选是否有科学意义。

### 4.3 后续 data2 迁移

赛事 data2 是 DYAMOND C1440-LLC2160：GEOS 大气模型约 7 km、72 层，MITgcm 海洋模型约 2--4 km、90 层，模拟从 2020-01-20 开始约 14 个月。KuroshioVis 附件已经证明可用 OpenVisus 读取选定时间、空间、深度和质量级别的子集，并使用 SSH 检测涡旋、SST/LHF/风异常、涡旋集合和滞后回归组织证据。迁移时保持推荐器接口不变，只替换数据适配器、特征提取器和领域任务模板。

## 5. 状态、动作与候选生成

### 5.1 分析状态

```json
{
  "variables": ["tas", "pr"],
  "region": {"lon": [130, 150], "lat": [25, 40]},
  "time": {"start": "2000-01-01", "end": "2010-12-31", "aggregation": "monthly"},
  "model": "MRI-ESM2-0",
  "scenario": "historical",
  "operation": "anomaly_and_correlation",
  "goal": "寻找黑潮区域的温度-降水共同变化"
}
```

### 5.2 第一版动作空间

- `add_variable` / `remove_variable`：增加或去掉 `pr`、`sfcWind`、`hurs` 等变量。
- `change_time_window`：日/月/季聚合，或向前/向后扩展时间窗。
- `change_region`：扩大区域、缩小到高响应区域、平移邻域。
- `change_model` / `change_scenario`：在同一时期比较模型或 SSP。
- `change_statistic`：均值、异常、相关、滞后相关、回归斜率、显著性。
- `compare_subset`：历史/未来、暖季/冷季、强风/弱风或高降水/低降水分组。

动作必须有参数范围、预计数据量、可撤销标识和唯一 ID；候选生成器不得生成超出数据适配器能力的动作。

## 6. “值得探索”的第一版定义

先使用可解释的加权评分，不训练模型：

```text
Score(a|S) =
  0.30 * Relevance
+ 0.25 * InformationGain
+ 0.15 * Novelty
+ 0.15 * EvidenceStability
+ 0.15 * (1 - NormalizedCost)
```

- **Relevance**：候选与用户目标、当前变量和领域关系的匹配程度。第一版由规则计算，例如目标包含“滞后响应”则优先滞后相关。
- **InformationGain**：候选是否可能改变当前判断；可用相关/回归结果变化、异常区域占比变化或不确定性下降近似。
- **Novelty**：与历史已探索状态的差异，避免重复推荐。
- **EvidenceStability**：不同时间块、空间子块或 bootstrap 重采样下结果方向是否一致。
- **Cost**：预计下载字节数、读取点数、计算时间和缓存命中情况的归一化组合。

权重不是最终真理。每次用户接受/拒绝候选时记录选择和理由，后续用小规模用户研究检验权重是否需要调整。系统必须同时展示各分项分数，避免只显示一个无法审计的总分。

## 7. 推荐界面与人机交互

建议做一个轻量 Panel/Streamlit 原型，包含：

1. 当前状态栏：变量、区域、时间窗、模型/情景、统计操作和目标。
2. 候选列表：最多 3 个，显示动作、总分、成本、证据稳定性和一句推荐理由。
3. 证据预览：当前结果与候选结果的并排地图/时间序列/散点图；标出样本数、缺测比例、置信区间或显著性。
4. 操作按钮：接受、编辑参数、拒绝并说明原因、回退、加入收藏。
5. 探索历史：状态图或时间线，支持点击任意节点恢复。
6. 解释面板：明确区分“数据计算结果”“规则推断”“LLM 生成的自然语言”，LLM 不得修改数值结果。

推荐时机先采用混合主动：用户完成一次分析动作后给出候选；若计算成本高，先显示预估成本和取消按钮；只有在结果稳定且候选明显不同于历史时才主动提示。不要在用户连续操作过程中频繁弹窗。

## 8. 实现步骤

### 阶段 A：数据适配与可复现基线（第 1 周）

1. 阅读赛事 data3 页面、Microsoft Planetary Computer 说明和官方 Quarto 示例，确认下载 API、文件格式、时间维度和许可。
2. 写 `data/manifest.yaml`，记录 URL、变量、模型、情景、时间范围、区域、单位、版本、校验和。
3. 只下载一个变量、一个模型、一个小区域和短时间窗；完成坐标、单位、缺测值、经度范围和时间排序检查。
4. 生成 `data/sample/` 中可公开提交的小型派生样例，原始数据放在 `.gitignore` 指定的本地目录。

### 阶段 B：候选动作与评分（第 2 周）

1. 实现 `AnalysisState`、`Action`、`Candidate` 数据结构。
2. 实现动作合法性检查、缓存键和估算成本。
3. 为每个候选运行统一统计管线，产出指标 JSON 和小图。
4. 实现评分函数，并输出每一项分数及其依据。
5. 建立无推荐基线：按固定顺序或随机选择动作，用于后续比较。

### 阶段 C：交互闭环（第 3 周）

1. 实现当前视图、候选卡片、证据预览、接受/拒绝/回退和历史记录。
2. 每次转移写入 JSONL：`state_before`、`action`、`state_after`、候选分数、用户选择、拒绝原因、耗时。
3. 增加数据质量警告：缺测过多、样本数过少、空间/时间切片为空、模型或情景不一致。

### 阶段 D：小规模评估（第 4--5 周）

1. 用 10--20 个预定义分析目标跑离线回放，比较推荐器与随机/固定顺序基线。
2. 由自己和至少一位了解气候/海洋数据的合作者完成任务，记录接受率、重复率、单步耗时、回退率和目标相关性评分。
3. 对候选理由做事实核查：每句话都能追溯到状态、统计量、数据切片或明确规则。
4. 做消融：去掉成本、去掉新颖性、去掉稳定性，观察推荐质量和用户负担变化。

### 阶段 E：迁移 DYAMOND（验证后）

1. 用 OpenVisus 读取 data2 的有限时空深度块；保留质量级别和读取成本。
2. 增加 SSH/涡旋特征、SST/LHF/风异常、涡旋类别和大气滞后统计。
3. 将 KuroshioVis 的“粗定位 -> 集合复合 -> 跨界流 -> 证据矩阵”作为候选视图和证据来源，而不是直接复制其推荐逻辑。
4. 与 data3 原型对照，分析推荐器在高分辨率、跨大气/海洋网格和稀疏证据下是否仍然可靠。

## 9. 评价指标与实验记录

### 9.1 推荐质量

- Top-3 候选中被用户接受的比例。
- 用户认为“有科学意义”的候选比例。
- 与随机、固定顺序和仅相关性排序基线的差异。
- 单步后目标相关指标的改善或不确定性下降。
- 重复推荐率、无效候选率、因成本取消率。

### 9.2 人机协同

- 完成一个目标所需步数和总时间。
- 用户回退率、编辑率、拒绝原因分布。
- 用户对推荐理由的正确理解率和信任校准。
- 用户是否能区分相关性、统计显著性与因果解释。

### 9.3 可复现性

每个结果保存：数据版本、查询参数、代码版本、随机种子、统计方法、样本数、缺测处理、运行时间和硬件信息。所有图表从 JSON 指标重新生成，不把截图当作唯一证据。

## 10. 风险与应对

| 风险 | 应对 |
|---|---|
| data3 实际访问结构与网页描述不一致 | 先做元数据探查和极小读取；把 API 适配器与研究逻辑分离 |
| 相关性被误读为因果性 | UI 和报告中明确写“探索性关联”；显示样本量、置信区间和限制 |
| 推荐分数看似精确但缺乏依据 | 展示分项分数、规则、数据证据和不确定性 |
| 下载/计算成本过高 | 区域、时间、模型和分辨率分级；预览后再精算；缓存中间结果 |
| LLM 幻觉或篡改分析结论 | LLM 只做目标解析和语言组织；数值结果来自受控函数；输出引用证据 ID |
| 领域 gap | 先让海洋专家审阅任务模板和推荐理由，再迁移 data2 |
| 小样本评估结论不稳 | 先报告原型和限制；扩大任务与专家数量后再主张泛化 |

## 11. 仓库组织建议

```text
SCV/
├── EXPLORATION_RECOMMENDATION_PLAN.md
├── README.md
├── data/
│   ├── manifest.example.yaml
│   └── sample/                 # 仅放许可允许且足够小的派生样例
├── src/
│   ├── adapters/               # NEX-GDDP；后续 DYAMOND
│   ├── state.py
│   ├── actions.py
│   ├── evidence.py
│   ├── ranking.py
│   └── history.py
├── app/                        # Panel/Streamlit 原型
├── experiments/
│   ├── configs/
│   └── logs/
└── docs/
    ├── data_dictionary.md
    └── evaluation_protocol.md
```

## 12. 与导师讨论时需要确认的三个问题

1. 第一轮是否接受“data3 做推荐框架原型、data2 做海气领域验证”的两阶段定位？
2. 在黑潮区域，优先让专家判断哪些目标和证据最有意义：变量组合、时间滞后、空间尺度，还是模型/情景差异？
3. 小实验阶段是否可以找到一位海洋/气候领域合作者，帮助标注候选的科学相关性和推荐理由是否可信？

## 13. 当前完成标准

当以下条件同时满足时，第一阶段算完成：

- 给定一个分析状态，系统能稳定生成不超过 3 个合法候选。
- 每个候选有可复现的证据、成本估计、分项评分和解释。
- 用户可以接受、拒绝、编辑和回退，历史记录可重放。
- 在至少 10 个目标上，推荐器相对随机基线有可解释的改进，或明确发现评分设计无效并记录原因。
- 不需要下载全量 28 TB 数据，且从原始数据到结果的过程可以由另一台机器复现。

## 参考资料

- SciVis Contest 2026 data3：<https://sciviscontest2026.github.io/data/home#data3>
- SciVis Contest 2026 data2 / DYAMOND：<https://sciviscontest2026.github.io/data/home#data2>
- DYAMOND OpenVisus notebook：<https://github.com/sci-visus/sciviscontest2026/blob/main/notebooks_examples/ieee_scivis_dyamond_ocean.ipynb>
- Microsoft Planetary Computer NEX-GDDP CMIP6：<https://planetarycomputer.microsoft.com/dataset/nasa-nex-gddp-cmip6#overview>
- 本地附件：导师提交的 KuroshioVis report（`SciVisContest26.pdf`）与《大模型赋能的可视化与可视分析研究综述》（PDF）。

