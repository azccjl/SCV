[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "scv-exploration-core"
version = "0.1.0"
description = "Lightweight candidate recommendation core for SCV"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["src"]
